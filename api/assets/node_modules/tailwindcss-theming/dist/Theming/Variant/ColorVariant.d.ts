import { TinyColor } from "@ctrl/tinycolor";
import { Variant } from "./Variant";
export declare class ColorVariant extends Variant {
    originalInput: string;
    color: TinyColor;
    constructor(name: string, value: string, colors?: string[]);
}
