"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("../../..");
function getCssThemeName(theme, config, keepName = false) {
    if (!keepName && theme.isDefault()) {
        return ':root';
    }
    let name = theme.getName(); // tailwind should pascal-case this
    switch (config.strategy) {
        case __1.Strategy.Attribute:
            return `[${name}]`;
        case __1.Strategy.PrefixedAttribute:
            if (config.prefix) {
                return `[${config.prefix}-${name}]`;
            }
            else {
                throw new Error('Strategy is set to prefixed attribute but no prefix is defined.');
            }
        case __1.Strategy.DataThemeAttribute:
            return `[data-theme=${name}]`;
        case __1.Strategy.DataAttribute:
            return `[data-${name}]`;
        case __1.Strategy.Class:
            return `.${name}`;
        case __1.Strategy.PrefixedClass:
            if (config.prefix) {
                return `.${config.prefix}-${name}`;
            }
            else {
                throw new Error('Strategy is set to prefixed class but no prefix is defined.');
            }
        default:
            throw new Error(`Unknown strategy: ${config.strategy}.`);
    }
}
exports.getCssThemeName = getCssThemeName;
