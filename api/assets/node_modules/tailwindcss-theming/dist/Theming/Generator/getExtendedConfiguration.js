"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
const get_1 = require("./get");
function getExtendedConfiguration(themes, config) {
    const extendConfiguration = {};
    const properties = utils_1.getDefaultTheme(themes)
        .getCustomProperties()
        .filter(prop => prop.extends());
    properties.forEach(property => {
        get_1.set(extendConfiguration, property.getPath(), `var(${utils_1.getCustomPropertyVariableName(property)})`);
    });
    return extendConfiguration;
}
exports.getExtendedConfiguration = getExtendedConfiguration;
